<div class="container" >
  <header class="image-bg-fluid-height" src="<%= @header_image %>">
    <div class="artist-name">
      <h3><%= @artist.artist_name %></h3>
      <a href="<%= @artist.artist_url %>">公式サイト</a>
    </div>
  </header>

  <div class="main-body">
  </div>
</div>